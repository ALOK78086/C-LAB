// Write a program to print Floyd’s triangle
using System;
class Program
{
    static void Main()
    {
        int num = 1;

        for (int i = 1; i <= 5; i++)
        {
            for (int j = 1; j <= i; j++)
                Console.Write(num++ + " ");

            Console.WriteLine();
        }
    }
}
